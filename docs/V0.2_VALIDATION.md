# v0.2 validation — 2026-09-21

## Executed

- Built the solution with the project's portable .NET SDK and locally installed Siemens V21 net48 Base/Step7 assemblies: zero errors, zero warnings on the final full source build.
- `tests/TiaAgent.Tests`: 22 checks passed for source declaration names, Chinese identifiers, comments/strings, same-line and multiline multi-object sources, mismatched source types, malformed source, XML object identity/type, multiple XML objects, prohibited DTD/XXE, volatile XML document metadata, compiler result consistency and preview token isolation/replay.
- `tests/test_stdio.py`: actual MCP initialize/tools-list/tools-call over stdio. Capabilities exactly matched registration: inspect=16, engineering=27, online=30, control=30.
- Host tools deny engineering writes in inspect; direct Worker requests independently deny engineering/online operations in inspect.
- Real Worker `get_compile_messages` before compile returns structured `no_compile_result`, not a serialization exception.
- Durable pending journal survived a host restart and blocked dispatch; `unknown` could not be recovered. `completed` could be acknowledged in inspect mode without replay.
- A fake Worker delayed replies beyond a 1000 ms configured timeout. Exactly one command was dispatched; a second call was blocked. The eventual result became `completed`, and recovery succeeded.
- A fake Worker returned the wrong request ID after a timeout. Journal remained `unknown`, with no redispatch.
- A real read-only `get_project_status` returned `worker_error: InvalidOperationException: No running TIA Portal process was found.` It did not launch an instance or open a project.
- Extracted all 51 registered reference tool names from the local InoProShop source and checked that each has a row in the comparison document: none missing.

## Live V21 acceptance — CPU 1215C, 2026-09-21

- Created and saved `C:/PLC/TestProjects/TiaMcp_CPU1215C_Acceptance/TiaMcp_CPU1215C_Acceptance.ap21` through the actual MCP host and Openness worker.
- Queried the installed catalog for exact order number `6ES7215-1AG40-0XB0`. All eight matches were returned; highest catalog firmware was V4.7. Added `PLC_1215C` / `CPU_1215C` using `OrderNumber:6ES7 215-1AG40-0XB0/V4.7`. Read back the CPU's order number and `FirmwareVersion=V4.7` from native attributes.
- Created a program folder, SCL FC `Mcp_Add`, global DB `Mcp_Data`, UDT `Mcp_State`, tag table `Mcp_Acceptance` and Bool tag `Mcp_TestInput` at `%I0.0` through preview/apply transactions.
- Exported FC, DB, UDT, tag table and the default LAD Main OB as XML. Read FC source, batch source and literal search results; the LAD OB correctly produced explicit incomplete/read-error metadata for text-only operations.
- Read OB configuration, empty project library and compile history. Final PLC software compile: Success, 0 errors, 0 warnings. Saved successfully.
- New or modified Siemens blocks must be compiled before XML export. Initial acceptance attempts stopped on Siemens' inconsistent-block export error without applying the pending change. The live script now compiles initially and after each applied change. Compilation remains outside the transaction.
- Reproduction scripts: `scripts/create_acceptance_project.py` and `scripts/acceptance_cpu1215.py`. Evidence: `artifacts/cpu1215-acceptance/creation.jsonl`, `cpu-selection.json`, and `acceptance-20260921-192302.jsonl`. Run under the Windows user with Siemens Openness membership. Creation refuses to add another CPU to a nonempty project; acceptance skips existing named fixtures.

## Not executed

No XML re-import/round trip, rename/move/archive, real transaction rollback or concurrent manual-edit scenario inside TIA, hardware compile, PLC connection/download/variable write/STOP/RUN were exercised. The tested LAD block was the default Main OB, not a representative graphical program. This does not prove preservation of arbitrary graphical content or complete reference integrity. Test fixtures are offline and are not called from Main.

The reference project's own README also distinguishes offline tests from live InoProShop acceptance. This comparison does not assume the reference's exposed tools have all been physically verified.

## Repeat

Run `scripts/test.ps1` after dependency restore. Local offline restore can use an existing NuGet cache with explicit `--packages` and `--source`; a new installation needs NuGet access. Test projects themselves have no external package dependencies. Fake Worker is test-only and is not included in the normal host worker package.

Before release for real engineering use, follow the live acceptance sequence in `V0.2_PARITY.zh-CN.md` using a disposable V21 project.
